# Kosu monorepo CI config file (last updated: 24 June 2019)

kind: pipeline
name: tests

steps:
  - name: prettier-project
    image: gcr.io/kosu-io/node-lts:latest
    pull: always
    environment:
      WEB3_URI: http://kosu-geth:8545
    commands:
      - yarn prettier:ci
    depends_on:
      - clone

  - name: build-project
    image: gcr.io/kosu-io/node-lts:latest
    pull: always
    environment:
      WEB3_URI: http://kosu-geth:8545
    commands:
      - yarn
      - yarn setup:ci
      - cd packages/kosu-system-contracts
      - yarn migrate:ci
    depends_on:
      - clone

  - name: npm-tests
    image: gcr.io/kosu-io/node-lts:latest
    pull: always
    environment:
      WEB3_URI: http://kosu-geth:8545
    commands:
      - yarn test:ci
    depends_on:
      - build-project

  - name: solidity-tests
    image: gcr.io/kosu-io/node-lts:latest
    pull: always
    environment:
      WEB3_URI: http://kosu-geth:8545
    commands:
      - yarn contracts:test:ci
    depends_on:
      - build-project

  - name: go-kosu
    image: gcr.io/kosu-io/go-kosu-ci:latest
    pull: always
    environment:
      WEB3_TEST_URI: ws://kosu-geth:8546
    commands:
      - cd packages/go-kosu
      - cat ../kosu-system-contracts/src/deployedAddresses.json
      - make ci
    depends_on:
      - build-project

services:
    - name: kosu-geth
      image: gcr.io/kosu-io/kosu-geth:0.1.1
      pull: always
      ports:
          - 8545
          - 8546

trigger:
    event:
        - pull_request

# UNTESTED
# ---
# kind: pipeline
# name: go-kosu-release
#
# release:
#     image: golang:1.12
#     secrets: [github_token]
#     commands:
#      curl -sL https://git.io/goreleaser | bash
#     when:
#       event: tag
