kind: pipeline
name: default

steps:
  - name: node-tests
    image: node:lts
    commands:
    - sudo apt-get update
    - sudo apt-get install -y netcat
    - yarn global add typescript
    - yarn
    - yarn build
    - yarn test