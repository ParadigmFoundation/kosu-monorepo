const KosuConnect = require('../dist').default;

describe('KosuConnect', () => {
  describe('constructor()', () => {
    // it("should create an ropsten infura web3 when no provider is given", async () => {
    //   //TODO This overides the Overrides
    //   const testKosu = new KosuConnect();
    //
    //   testKosu.should.be.instanceOf(KosuConnect);
    //   (await testKosu.web3.eth.net.getId()).should.eq(3)
    // });
  });
});
