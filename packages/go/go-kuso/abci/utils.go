package abci

import (
	"go-kuso/abci/types"
)

// GenLimits generates a rate-limit mapping based on staked balances and the total order limit per staking period.
func GenLimits(rls []*types.RateLimit) {

}
